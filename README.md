# laravel-CRUD-with-query-builder
Laravel CRUD Application using Query Builder — A simple and clean example demonstrating Create, Read, Update, and Delete operations in Laravel using the Query Builder approach (without Eloquent ORM)
